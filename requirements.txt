# Development dependencies only - NOT used when users install the package
# Users get dependencies from pyproject.toml (Django>=3.0, Pillow>=8.0.0)

# Core (for local development)
Django>=3.0
Pillow>=8.0.0

# Testing
pytest>=7.0.0
pytest-django>=4.5.0
pytest-mock>=3.10.0
